{
  "name": "maker-one",
  "module": "src/index.ts",
  "type": "module",
  "version": "1.0.1",
  "description": "A CLI interface to create a new Capture One project and log it in Google sheets",
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --minify --outfile dist/maker-one",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.3",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@google-cloud/local-auth": "2.1.0",
    "chalk": "^5.3.0",
    "googleapis": "105",
    "inquirer": "^9.2.11",
    "lolcatjs": "^2.4.1",
    "nanospinner": "^1.1.0"
  }
}
